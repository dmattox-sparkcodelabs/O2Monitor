# O2 Monitor Requirements
# Install with: pip install -r requirements.txt
# Note: venv must be created with --system-site-packages for PyGObject/GLib
# Note: Upgrade pip first: pip install --upgrade pip
# Note: cffi and cryptography need --only-binary :all: for ARM prebuilt wheels

# Workaround for vext blocking system packages
cffi==1.17.1
cryptography==42.0.8
idna>=3.0

# BLE Communication (D-Bus based - works with Checkme O2 Max)
BLE_GATT>=0.5.0
pydbus>=0.6.0

# Smart Plug
python-kasa>=0.5.0

# Audio
pygame>=2.5.0

# Web Framework
flask>=2.3.0
flask-login>=0.6.0

# Security
bcrypt>=4.0.0

# HTTP/API
aiohttp>=3.8.0
requests>=2.31.0

# Configuration
python-dotenv>=1.0.0
pyyaml>=6.0
ruamel.yaml>=0.17.0

# Database
aiosqlite>=0.19.0

# Templating (usually included with Flask, but explicit)
jinja2>=3.1.0

# Scheduling
apscheduler>=3.10.0
